# --- Postgres ---
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# --- Redis ---
# (no auth needed for local dev; add REDIS_PASSWORD + command flags if you want to lock it down)

# --- MinIO (S3-compatible storage) ---
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
MINIO_BUCKET=

# --- Derived URLs your apps will use (Phase 2+) ---
DATABASE_URL= 
REDIS_URL= 
S3_ENDPOINT=
S3_ACCESS_KEY_ID=  
S3_SECRET_ACCESS_KEY= 
S3_BUCKET=
S3_REGION=
